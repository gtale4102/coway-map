# 고객사 관리 웹앱 (고객사 지도)

> **세션을 마칠 때마다 이 CLAUDE.md의 "현재 진행 상태" 섹션을 그날 작업 내용으로 갱신하고,
> 아래를 저장소·실제 파일과 기계적으로 대조해 누락을 확인할 것.**
>
> 1. 프로젝트의 스크립트·문서 파일이 이 CLAUDE.md의 문서/도구 안내에 빠짐없이 있는가
> 2. **CLAUDE.md 에 적힌 파일이 실제로 존재하는가** — 반대 방향도 확인한다
> 3. CLAUDE.md 가 가리키는 화면 이름·설정 키·수치가 현재 코드·산출물과 맞는가
>
> **기억으로 적지 말고 명령으로 대조할 것.** "그날 작업 내용"만 적는 방식은
> 진행 상태 밖의 목록형 섹션(파일 목록, 문서 안내, 스키마)이 조용히 낡는 것을
> 막지 못한다. 대조 항목은 프로젝트 성격에 맞게 늘려도 되지만 줄이지 않는다.
>
> **묶음 표기(`verify_*.py` 전체·`tests\` 등)와 `(대화N)` 같은 패턴 표기는
> 개별 파일명이 없어도 위 항목을 충족한 것으로 본다.** 파일명 단위로 대조하면
> 계속 미언급으로 잡히지만 정상이며, 묶음을 풀어 나열할 필요는 없다.

## 범위와 승인

### 작업 범위 준수 원칙
> 최초 작성: 2026-07-08 / 최근 수정: 2026-07-11

요청받은 작업 범위 밖의 파일은 먼저 확인 없이 수정하지 말 것. 작업 중 관련 있어 보이는 다른 코드를 발견하더라도, 그 자리에서 임의로 범위를 넓혀 고치지 말고 반드시 먼저 태일님에게 물어보고 승인을 받은 후에 진행할 것. 이는 아래 "연동과 통합" 카테고리의 "연동 제안 원칙"과는 별개다 — 다른 프로젝트와의 연동 가능성을 발견하면 알리는 것은 적극적으로 하되, 실제로 코드를 고치는 것은 승인 후에만 한다.

수정하기 전에는 어떤 파일을 어떻게 바꿀 것인지 계획을 먼저 보여주고 승인을 받을 것. Git으로 관리되는 프로젝트는 작업을 시작하기 전에 현재 상태를 커밋해둘 것. 작업이 승인받은 대로 완료되면, 그 변경사항도 의미 있는 커밋 메시지와 함께 커밋할 것. 커밋 전에 무엇을 커밋할지 태일님에게 보여주고 확인받은 뒤 진행한다. Git이 없는 프로젝트는 이런 안전장치가 없다는 점을 감안해 더 신중하게 작업할 것.

## 연동과 통합

### 통합 플랫폼 참조 원칙
> 최초 작성: 2026-07-08 / 최근 수정: 2026-07-08

이 프로젝트는 코웨이 B2BC 영업 통합 플랫폼의 일부다. 새 기능을 추가하거나 이 프로젝트와 관련된 새 프로젝트를 만들 때는 반드시 통합 플랫폼 상위 폴더의 `ARCHITECTURE.md`를 먼저 읽고 그 원칙(데이터 연동 방식, 폴더 배치 규칙)을 따를 것. 이 플랫폼은 지금 있는 프로젝트들로 고정된 게 아니라, 앞으로 새 프로젝트가 계속 추가되고 기존 프로젝트가 업그레이드되면서 하나로 유기적으로 묶이는 걸 전제로 설계됐다는 걸 항상 염두에 두고 작업할 것.

#### 연동 제안 원칙
> 최초 작성: 2026-07-08 / 최근 수정: 2026-07-08

이 프로젝트에서 작업하다가 다른 프로젝트의 데이터나 기능과 연동하면 유용할 것 같은 지점을 발견하면, 작업 도중이든 끝났을 때든 먼저 태일님에게 제안할 것. 새 기능이 다른 프로젝트가 이미 갖고 있는 데이터를 활용할 수 있어 보이거나, 지금 만드는 로직이 다른 프로젝트에도 적용하면 좋을 것 같으면, 묻지 않아도 먼저 알려줄 것.

#### 연동 자동 갱신 원칙
> 최초 작성: 2026-07-08 / 최근 수정: 2026-07-11

다른 프로젝트와 데이터를 주고받는 연동을 새로 만들거나 수정했다면, 별도 지시가 없어도 예외 없이 그 작업이 끝나는 즉시 아래 두 가지를 자동으로 실행할 것:
- (a) 이 프로젝트의 CLAUDE.md와 연동 대상이 된 다른 프로젝트의 CLAUDE.md에 이 연동 내용을 기록
- (b) 상위 폴더의 `ARCHITECTURE.md`에도 이 연동 관계를 추가

**이는 위 "범위와 승인" 카테고리의 "작업 범위 준수 원칙"의 예외로, 연동 사실을 CLAUDE.md/ARCHITECTURE.md에 기록하는 행위에 한정된다.** 문서 갱신은 승인 없이 즉시 진행하되, 연동을 위해 다른 프로젝트의 실제 코드·로직을 수정하는 것은 이 예외에 포함되지 않으며 "작업 범위 준수 원칙"에 따라 사전 승인이 필요하다.

이 작업(문서 기록)을 하지 않고 세션을 마치는 것은 금지된 행동이다.

## 산출물 기준

### 디자인·소통 원칙
> 최초 작성: 2026-07-08 / 최근 수정: 2026-07-11

이 프로젝트에서 화면, 문서, 보고서 등 사람이 직접 보는 산출물을 만들 때는 사용자 편의성을 최우선으로 한 심플하고 간결한 UI/UX를 갖추고, 코웨이의 상징색을 위주로 세련되고 깔끔하게 디자인할 것. 단, 시각적 산출물이 없는 순수 백엔드 스크립트나 자동화 로직에는 이 디자인 원칙이 해당하지 않는다. 작업 대화는 반말로 진행하되, 실제로 산출되는 결과물(문서, 보고서, 코드 주석 등)에는 사무용 경어체를 사용할 것.

진실성 원칙(확인되지 않은 내용을 사실처럼 단정하지 않기 등)은 상위 `ARCHITECTURE.md`의 "진실성 원칙" 섹션을 따른다 — 모든 프로젝트 공통이므로 여기서 반복하지 않는다.

## 프로젝트 개요

태일님(대표)·정열님(동료) 두 사람이 함께 쓰는 코웨이 B2BC 영업용 내부 도구. 법인 고객사의 위치·컨택이력·연락처를 지도 위에서 관리한다.

- 단일 파일 `index.html`(HTML+CSS+JS, 프레임워크 없음, 수천 줄), 카카오맵 JS API + 카카오 모빌리티 REST API + 다음 우편번호 API
- 데이터 저장소: Google Sheets 한 셀(A1)에 전체 JSON 통째 저장, Google Apps Script 웹앱이 저장 엔드포인트. 실패 시 `localStorage` 폴백(오프라인 모드)
- 배포: GitHub Pages (`gtale4102/coway-map`, **public 저장소**), 커스텀 도메인 `map.b2bc-coway.com`
- **고객사 관리 문자 발송 자동화** 프로젝트가 이 앱의 Google Sheets 데이터를 읽기 전용으로 연동해 담당자 연락처를 가져간다 (`SCHEMA_CONTRACT.md`로 의존 필드 명시)

customer 데이터 구조(v5.00 이후 고정): `{ id, name, category, memo, addresses: [{id, label, address, detail, lat, lng, contactName, contactPhone}], lat, lng, owner('태일'|'정열'), logs: [...], createdAt }`. 담당자는 **주소마다 1명**이며, 과거 있던 고객사 레벨의 공용 `contacts[]` 배열은 v5.00에서 완전히 제거되고 `addresses[].contactName/contactPhone`로 통합됨(레거시 레코드는 로드 시 자동 마이그레이션).

고객사 최상위의 `lat`/`lng`는 `addresses[]`와 별개로 남아 있는 옛 구조의 **대표 좌표**다. 주소를 특정하지 않고 부르는 지도 이동(`moveToMarker`)·검색 결과 클릭(`selectSearchResultAddr`)의 기본값이고, 로드뷰(`openRoadview`)는 아예 이 값을 우선 쓰며, `renderAllMarkers()`의 구버전 호환 분기는 `addresses[]`에 좌표가 하나도 없을 때 이 값으로 핀을 대신 그린다. **값은 항상 "남은 주소 중 첫 좌표, 없으면 `null`"로 유도한다** — `splitAddress()`·`submitForm()`·재가져오기(v5.56~v5.57)가 모두 같은 패턴으로 재계산한다. 주소를 건드리는 새 경로를 만들 때마다 이 재계산을 빠뜨리지 말 것(실제로 재가져오기 쪽에서 두 번 연속 빠져 v5.56·v5.57로 나눠 고쳤다).

### 파일 구성

| 파일 | 역할 |
|---|---|
| `index.html` | **앱 본체.** HTML+CSS+JS 한 파일(약 6,460행, 프레임워크 없음) |
| `apps-script/Code.js` | **Google Sheets 저장 엔드포인트 본체.** `doGet`(조회·토큰 검증) / `doPost`(전체 저장 `handleFullSave_`, 컨택이력 1건 추가 `handleAppendLogData_`). **문자발송 프로젝트가 읽는 것이 바로 이 엔드포인트다**(ARCHITECTURE 2-4) |
| `apps-script/appsscript.json` · `apps-script/.clasp.json` | Apps Script 매니페스트·clasp 배포 설정 (둘 다 `apps-script/` 아래에 있다) |
| `icon-192.png` · `icon-512.png` | 파비콘·홈화면 아이콘 (`index.html`이 `rel="icon"`/`apple-touch-icon`으로 직접 참조. 별도 `manifest.json`은 **없다**) |
| `dulcet-medley-...json` | Google 서비스 계정 키 — **`.gitignore`로 git 제외**. 이 앱 자체의 구글 클라우드 프로젝트 인증 파일이며 다른 프로젝트와 무관하다 |
| `CNAME` | GitHub Pages 커스텀 도메인 (`map.b2bc-coway.com`) |
| `.gitignore` | git 제외 목록 — 서비스 계정 키(`dulcet-medley-...json`) · `.claude/` · `render*.png` |
| `고객사_일괄등록_양식.xlsx` · `고객사목록_양식.xlsx` | 고객사 일괄 등록·목록 입력 양식 |
| `render*.png` | 렌더 비교용 스크린샷(v2.92 / v3.00) — **로컬 전용, `.gitignore`로 git 제외.** 실제 고객사명·건물명이 찍혀 있어 public 저장소에 올리지 않는다(2026-08-22에 히스토리에서 제거) |
| `md/handover-coway-SKILL.md` | **claude.ai 스킬 정의.** "인수인계 준비해줘"·"새 채팅창으로 넘어갈게" 등에 반응해 인수인계 문서를 자동 생성한다 |
| `md/` | 대화 기록 문서 모음(`인수인계_요약 (대화N).md` · `통합설계_참고(대화N).md`)과 위 스킬 정의. 아래 "문서 역할 안내" 참고 |
| `README.md` | GitHub 저장소 표시용(내용은 제목 한 줄뿐). **GitHub 규약상 루트 고정** |
| `CLAUDE.md` | 이 문서. **Claude Code가 프로젝트 루트에서 자동 로드하므로 루트 고정** — `md/`로 옮기면 여기 적힌 규칙이 세션에 실리지 않는다 |

**`apps-script/Code.js` 는 이 저장소의 사본이고 실제로 도는 것은 Google Apps Script 쪽에 배포된 코드다** — 한쪽만 고치면 어긋난다.

## 핵심 규칙

**불확실한 건 임의로 해석해서 코드에 넣지 말 것. 확인이 필요하면 먼저 물어볼 것.** 특히 삭제를 수반하는 로직이나 스키마 변경은 반드시 설계안을 먼저 보고하고 승인받은 뒤 구현한다.

이 프로젝트에서 실제로 사고/버그로 이어졌던, 절대 빠뜨리면 안 되는 규칙들:
- **삭제 가능한 모든 컬렉션은 삭제기록(tombstone)이 있어야 한다** — 로컬↔구글시트 병합은 "상대에만 있는 항목은 추가, 삭제는 안 함"이 기본이라, tombstone이 없으면 삭제한 항목이 병합 시 되살아난다. 과거 이 패턴을 빠뜨려 동료 고객사 데이터 20~30건이 영구 소실된 사고가 있었다. 새 삭제 가능 기능을 추가할 때마다 이 패턴을 반드시 복제할 것 (**현재 `trash`(휴지통) 완전삭제에는 이 tombstone이 없는 상태로 남아있음 — 아래 "다음 할 일" 참고**).
- **새 팝업/화면을 추가하면 `closeTopmostLayer()`와 `pushNavGuard()` 두 곳에 반드시 등록**할 것 — 빠뜨리면 그 화면에서만 뒤로가기가 먹통이 된다.
- **이 저장소는 public이다** — 실제 개인정보(고객사명·전화번호·주소)나 API 키·서비스 계정 키가 담긴 파일은 커밋 전 반드시 확인한다 (과거 카카오 API 키가 평문 노출되어 레닥션 후 재커밋한 이력 있음).
- **customers/addresses의 필드명(owner, addresses[].contactName/contactPhone, name, category, memo, id)을 바꾸거나 삭제할 계획이 있으면, 먼저 `고객사 관리 문자 발송 자동화/SCHEMA_CONTRACT.md`를 확인하고 같은 세션에서 그쪽 `연락처_저장소.py`도 함께 갱신**할 것 — 과거 스키마를 바꾼 뒤 이걸 안 해서 문자발송 쪽 연락처 조회가 전면 무응답이 된 사고가 있었다.
- **같은 담당자가 여러 주소에 등록된 것을 "중복 데이터"로 보고 정리하거나 막지 말 것** — 담당자는 주소 1개당 1명 구조라, 한 담당자가 본원·분원처럼 여러 지점을 맡는 경우 같은 이름·전화번호가 여러 주소 블록에 들어가는 게 정상이다. 소비자인 `고객사 관리 문자 발송 자동화`가 (담당자명+전화번호) 기준으로 한 사람으로 병합해 쓴다(2026-07-28). 상세는 그쪽 `SCHEMA_CONTRACT.md`의 "같은 담당자가 여러 주소에 등록되는 것은 정상이다" 항목.
- 실 라이브 데이터로 테스트할 때는 반드시 테스트 전용 더미 레코드를 만들어 검증하고, 끝나면 완전히 정리(원래 건수 재확인)할 것 — 실제 고객사 데이터를 테스트 대상으로 쓰지 않는다.
- 매 작업마다 버전 번호(설정 화면 헤더 표시)를 규칙에 맞게 올릴 것 (정수부=구조적 큰 변화, 소수 첫째자리=중간 규모 기능, 둘째자리=버그픽스).
- Claude Code의 60초 무응답 자동 진행(AFK) 상황이 발생하면, 되돌리기 쉬운 보수적 선택을 하고 다음 보고에 그 사실을 반드시 명시할 것.

## 현재 진행 상태

**2026-08-22 (4):** 재가져오기 **엑셀 날짜 값 파싱 오류 수정**(v5.58). `handleReimportFile()`의
`sheet_to_json()` 두 곳(`existingRows`/`newRows`)이 `raw` 기본값(true)으로 읽어서, 사용자가 컨택날짜 칸에
"8-22"처럼 입력해 **엑셀이 진짜 날짜 값으로 자동 변환**하면 셀이 날짜 문자열이 아니라 내부 일련번호
(2026-08-22 → `46256`)로 들어왔다. 뒤따르는 파싱(`instanceof Date` 체크 / `yyyy-mm-dd` 정규식)이 숫자를
전혀 처리하지 못해 **경고 없이 `"46256"`이 그대로 컨택날짜로 저장**됐다(수정 전 코드로 실측 재현함).
두 호출에 `raw: false`만 추가했다 — 내보내기 때 날짜 칸에 이미 `numFmt: 'yyyy-mm-dd'`가 걸려 있어
서식대로 문자열화되므로, **진짜 날짜 값이든 텍스트로 친 값이든 똑같이 `"2026-08-22"`로 들어오고
기존 정규식 파싱이 그대로 맞물린다**(별도 파싱 코드 없음). `metaRows`(5085줄)는 ID만 담아 손대지 않았다.

`raw: false`가 시트 전체에 적용되므로 **모든 칸을 두 방식으로 파싱해 셀 단위로 대조**했다 — 세 시트
통틀어 값이 달라진 칸은 딱 둘: ①컨택날짜(`number:46256` → `string:"2026-08-22"`, 의도한 수정)
②사용자가 텍스트 칸에 숫자만 입력해 엑셀이 숫자 셀로 만든 경우(`number:1098765432` →
`string:"1098765432"`, 자릿수 동일). ②는 **오히려 잠재 버그를 없앴다** — 이전에는 그 number가
`(r['담당자 전화번호'] || '').trim()`에서 `TypeError: ... .trim is not a function`을 내
**파일 전체가 "파일을 읽을 수 없습니다"로 실패**했다(실측 확인). 고객사명·주소·상세주소·분류·컨택유형·
`_체크섬`·`_고객사ID`/`_주소ID` 등 나머지 칸은 전부 문자열 셀이라 값·타입 모두 변화 없음.

검증은 로컬 서버 + 브라우저에서 **테스트 전용 더미 고객사 3건**으로, 앱의 실제 내보내기로 만든 xlsx를
ExcelJS로 "엑셀에서 편집한 것처럼" 고친 뒤 **실제 파일 입력(`#reimport-file`) 경로**로 넣어 수행했다.
①진짜 날짜 값 ②텍스트 `yyyy-mm-dd`(회귀) ③분류·컨택유형 같은 검증 걸린 칸 ④텍스트 칸(전화번호 앞
`0` 보존 포함)을 모두 확인했고, 신규 추가 시트 경로(4649~4653줄)까지 포함해 반영 실행 후 저장된
`logs[].date`가 `2026-08-22`/`2026-08-20`/`2026-08-21`로 정확히 들어갔다. 저장·조회 함수는 스텁으로
차단하고 fetch/XHR/form 전송을 감시했으며 **구글시트 쓰기 요청 0건**, localStorage에도 더미 유출 없음.

**2026-08-22 (3):** 재가져오기 **주소 수정·추가·병합에서도 대표 좌표 재계산**(v5.57). v5.56이 삭제
경로만 막아서 나머지 세 경로에 같은 버그가 남아 있었다 — 주소 텍스트를 고치면 재지오코딩으로
`addresses[].lat`은 바뀌는데 고객사 `lat`/`lng`는 옛 좌표 그대로였고, 주소 0개 고객사에 주소를 처음
추가해도 `null`인 채라 주소 미지정 `moveToMarker` 호출이 "주소 또는 좌표가 없습니다"를 띄웠다.
v5.56이 만든 Set + 마지막 재계산 루프를 그대로 넓혀 쓴다: `addrDeletedCustIds` → `coordAffectedCustIds`로
이름을 바꾸고 선언을 카운터 옆으로 올린 뒤, ①이름중복 merge ②`customerChanges`의 주소 수정/추가
③`conflicts`의 주소 수정/추가 ④주소 삭제 네 지점에서 고객사 id만 담는다. **재계산 코드는 여전히
루프 끝의 한 곳뿐**이라, 한 고객사에 삭제+수정+추가가 겹쳐도 최종 `addresses[]` 기준으로 정확히
한 번만 계산된다(재계산 횟수 1회를 setter로 실측). `submitForm()`(일반 UI 편집)은 이미 매번 전체
재계산하므로 손대지 않았다.

**2026-08-22 (2):** 재가져오기 주소 삭제 시 **고객사 대표 좌표(`lat`/`lng`) 재계산**(v5.56).
v5.55 직후 발견된 별개 버그 — `applyReimportChanges()`의 `addressDeletions` 루프가 `addresses[]`에서만
주소를 빼고 고객사 최상위 `lat`/`lng`는 그대로 둬서, 지운 주소의 옛 좌표가 남았다. 이 필드는
`addresses[]`와 별개로 남아 있는 옛 구조 잔재인데 **"지도에서 보기"(`moveToMarker`)·검색 결과
클릭 이동(`selectSearchResultAddr`)이 주소를 특정하지 않고 호출될 때의 기본값**이고(검색 결과는
주소가 2개 이상이면 주소ID를 빈 문자열로 넘겨 이 폴백을 탄다), **로드뷰(`openRoadview`)는 아예
이 값을 우선 사용**하며, `renderAllMarkers()`의 구버전 호환 분기는 `addresses[]`가 전부 비면
이 좌표로 핀을 대신 그린다. 이제 삭제 루프 직후 `splitAddress()`와 동일한 패턴으로
`남은 주소 중 첫 좌표 → 없으면 null`로 재계산한다(영향받은 고객사 id를 Set에 모아 **고객사당 한 번만**).
`customerChanges`/`conflicts` 어느 쪽에서 온 삭제든 이 한 지점을 거치므로 중복 계산이 없다.
렌더링 로직은 건드리지 않았다 — `null`이 되면 구버전 호환 분기가 알아서 걸러진다.

**2026-08-22:** 엑셀 재가져오기에서 **주소 삭제를 독립 항목으로 분리**(v5.55). 기존에는 주소 삭제가
고객사 필드 수정·주소 추가/수정·컨택이력 추가와 함께 `customerChanges` 하나에 묶여 **기본 체크된**
체크박스로 실행됐고, 고객사 전체 삭제와 달리 휴지통 경로가 없어 되돌릴 수 없었다. 이제
`computeReimportDiff()`가 주소 삭제를 `result.addressDeletions[]`("고객사 하나의 주소 하나" 단위)로
빼내고, 미리보기가 이를 전용 섹션 "🗑️ 주소 삭제 예정 — 확인 후 체크해야 반영됩니다"에 **기본 미체크 +
복원 불가 경고**로 띄우며, `applyReimportChanges()`가 별도 루프로 체크된 것만 실행한다. 충돌
(`addrConflicts`) 케이스에서도 동일하게 분리했다 — 삭제는 충돌 여부와 무관하게 되돌릴 수 없으므로.
기존 "🗑️ 삭제 예정" 섹션 제목은 새 섹션과 구분되도록 "🗑️ 고객사 삭제 예정"으로 바꿨다(로직은 그대로).
검증은 로컬 서버 + 브라우저에서 **테스트 전용 더미 고객사**로 실제 xlsx를 만들어 실제 파일 입력 경로로
가져와 수행했고(저장·조회 함수는 스텁으로 차단, 구글시트 쓰기 요청 0건), 체크박스 독립 동작(수정만/삭제만/
둘 다/둘 다 해제 4가지)과 충돌 동시 발생·고객사 전체 삭제 경로까지 확인했다.
같은 날 별도 커밋으로 루트의 대화 기록 md를 `md/` 폴더로 정리했다(`README.md`·`CLAUDE.md`만 루트 유지).

**2026-07-11 (플랫폼 공통 작업):** 플랫폼 전역 CLAUDE.md 원칙 재구성 — "통합 플랫폼 참조/작업 범위 준수/디자인·소통" 평면 나열을 "범위와 승인/연동과 통합/산출물 기준" 3개 카테고리로 재편, 원칙별 최초 작성일·최근 수정일 표기 추가, 연동 자동 갱신 원칙에 작업 범위 준수 원칙과의 충돌 해소 문장 추가, 디자인·소통 원칙의 진실성 조항을 ARCHITECTURE.md로 통합 이관. 이 프로젝트 자체 변경 없음.

**2026-07-07 (플랫폼 공통 작업):** 이 CLAUDE.md 최상단에 "통합 플랫폼 참조 원칙"/"디자인·소통 원칙" 섹션 추가, 플랫폼 루트에 `ARCHITECTURE.md` 신설(현재 프로젝트 목록·연동 관계 정리, 이 프로젝트→`고객사 관리 문자 발송 자동화` 연동 포함). 이 프로젝트 자체 변경 없음.

**버전: v5.58** (2026-08-22 기준, 이후 갱신 없으면 이 상태가 최신 — 작업 시작 전 실제 파일의 설정 화면에서 버전 재확인할 것)

**완료:**
- 핀 겹침 펼치기(spiderfy), 라벨 충돌 회피, 클러스터링, 핀 병합(4m 이내), 데이터 분리/병합
- 엑셀 가져오기(신규 추가 전용)/양식 다운로드
- 동선짜기(카카오 모빌리티), 현재위치 이동, 고객사 목록 폴더화, 분류 7종(계약 중/친밀/보통/소원/발굴·가망/거점/미분류)
- **엑셀 내보내기 + 재가져오기(수정용) 신규 기능** (v5.20) — 기존 고객사를 엑셀로 대량 수정하는 완전히 새로운 워크플로우. ID 기준 매칭(숨김 컬럼), 완전 동일 데이터 자동 스킵, 컨택이력/좌표/생성일 보존(주소를 바꾸면 좌표는 재지오코딩됨), 실제 라이브 데이터(46개 고객사)로 왕복 검증 + 실제 반영까지 테스트 완료
- **재가져오기 삭제 항목 분리** (v5.55) — 되돌릴 수 있는 삭제(고객사 전체 → 휴지통)와 되돌릴 수 없는 삭제(주소)를 각각 **독립 섹션 + 기본 체크 해제**로 분리. 두 삭제 모두 체크하지 않으면 실행되지 않으며, 나머지 수정 항목(필드·주소 추가/수정·컨택이력)만 기본 체크 유지
- **주소 변경 시 대표 좌표 재계산** (v5.56 삭제 → v5.57 수정·추가·병합) — 재가져오기로 주소가 바뀌면 고객사 `lat`/`lng`를 최종 주소 기준으로 다시 잡는다. 영향받은 고객사 id를 `coordAffectedCustIds`에 모아 **모든 처리 루프가 끝난 뒤 고객사당 한 번만** 계산한다. 주소가 전부 없어지면 `null`이 되어 유령 핀이 그려지지 않는다
- **재가져오기 엑셀 날짜 값 파싱** (v5.58) — `sheet_to_json`을 `raw: false`로 읽어, 컨택날짜 칸이 진짜
  엑셀 날짜 값이어도(일련번호) 서식(`numFmt: 'yyyy-mm-dd'`)대로 `"2026-08-22"` 문자열로 들어온다.
  텍스트로 친 기존 방식과 결과가 같아 별도 파싱 코드가 없다
- 문자발송 자동화 쪽 스키마 불일치 사고 발견·수정(이 앱 자체 코드는 변경 없음, `SCHEMA_CONTRACT.md`/`MapSchemaError` 안전망은 그쪽에 추가됨)

**보류/미결:**
- **`SHEET_TOKEN` 클라이언트 노출 — 오리진 제한/토큰 회전 검토 필요** — 토큰이 `index.html`에 그대로 박혀 있어(`map.b2bc-coway.com` 소스에서 실제로 보임) 누구나 Apps Script 엔드포인트를 직접 호출해 전체 고객사 데이터를 읽을 수 있다. 순수 클라이언트 앱 구조상 원래 그런 것이며 특정 작업이 만든 문제는 아니다(2026-08-22 확인, 미조치)
- **재가져오기: "기존 고객사" 시트의 행을 전부 지우면 아무것도 처리되지 않음** — `handleReimportFile`이 두 시트가 모두 비면 "읽을 데이터가 없습니다"로 끝내서, 삭제 판정 자체가 돌지 않는다. 안전한 실패이긴 하나 사용자는 지운 대로 반영될 거라 기대할 수 있다(2026-08-22 발견, 미수정)
- **재가져오기: 행 삭제와 "칸만 비우기"의 결과가 한 경우에 다름** — 한 고객사의 행을 **전부 지우면** 고객사가 삭제 후보(휴지통 이동)가 되지만, 같은 행들을 **칸만 전부 비우면** 고객사는 남고 주소만 전부 삭제 후보가 된다. 그 외 경우(일부 행)에는 둘의 결과가 같다(2026-08-22 발견, 미수정)
- **재가져오기: 12자리 이상 숫자를 텍스트 칸에 입력하면 과학적 표기로 들어옴** — `raw: false`는 General
  서식 숫자를 엑셀과 같은 규칙으로 문자열화하므로, 11자리까지는 그대로지만(`1098765432` → `"1098765432"`)
  12자리부터는 `1012345678901` → `"1.01235E+12"`가 된다. 전화번호는 최대 11자리라 현실적으로 닿기
  어렵고, 수정 전(`raw: true`)에는 같은 입력이 `.trim()` TypeError로 **파일 전체 읽기 실패**였으므로
  더 나빠진 것은 아니다(2026-08-22 실측, 미조치)
- 기존 "엑셀 일괄 가져오기"(신규 추가 전용, `processExcelImport`)가 여전히 구식 `contacts[]` 스키마에 씀 — 자동 마이그레이션으로 문제는 없으나 정리가 필요함
- **휴지통(`trash`) 완전삭제에 tombstone이 없음** — 완전삭제 직후 다른 세션이 옛 trash 데이터를 병합해오면 되살아날 수 있는 버그, 발견되었으나 미수정
- 다중 분류(태그형), 종 모양 알림센터: 아이디어 단계에서 보류
- 담당자 기준 컨택이력 재설계: 데이터 모델 전체를 바꿔야 하는 큰 작업이라 보류, 현재 구조(주소 기준) 유지 중
- 엑셀 가져오기(기존, 신규추가용) 덮어쓰기/병합/추가 로직 개선: 사용자가 더 생각해보기로 보류

## 다음 할 일

1. 휴지통 완전삭제용 별도 tombstone 키(예: `PURGED_TRASH_IDS_KEY`) 추가 — `mergeAndSaveToSheet()`/`permanentlyDeleteFromTrash()` 수정
2. 기존 "엑셀 일괄 가져오기"가 `addresses[].contactName/contactPhone`에 직접 쓰도록 정리
3. (여유 있을 때) 다중 분류, 담당자 기준 컨택이력 재설계 여부 재검토

## 문서 역할 안내

대화 기록 문서는 `md/` 폴더에 모여 있으며 두 종류로 역할이 다르다:
- **`md/인수인계_요약 (대화N).md`**: 그 채팅에서 진행된 작업 전체를 새 세션이 그대로 이어받기 위한 **이력 요약**. 데이터 구조, 이번 세션에서 바뀐 것, 다음 체크리스트까지 시간 순으로 상세히 기록한다.
- **`md/통합설계_참고(대화N).md`**: 코드만 봐서는 알 수 없는 **배경 지식·시행착오·제약사항**을 정리한 참고 자료. 예를 들어 "왜 삭제 가능한 컬렉션마다 tombstone 패턴을 복제해야 하는지", "왜 엑셀 가져오기를 신규/기존 두 시트로 분리했는지" 같은, 코드 구조만 봐서는 그 이유를 알 수 없는 배경이 여기 담겨 있다.

작업 환경에 따라 방식이 다르다는 점도 참고: Claude Code(CLI, 로컬 파일 접근 가능) 환경이면 직접 파일 수정 + 로컬 서버(`python -m http.server`)로 브라우저 실검증까지 가능하고 이쪽이 더 안전하므로 우선한다. claude.ai(cowork) 환경이면 GitHub 직접 접근이 안 되므로 outputs 폴더에 저장 후 사용자가 직접 다운로드·push해야 한다.
