# 고객사 지도 앱 — 작업 인계 요약 (v5.01 기준)

이 문서는 직전 대화창(대화02 → 대화03)에서 이뤄진 모든 논의·결정·구현을 포함한 전체 맥락입니다. 새 대화창에서는 이 문서 하나만 보고 바로 이어서 작업할 수 있어야 합니다.

---

## 1. 프로젝트 개요

- **이름**: 고객사 지도 (코웨이 B2BC 영업용 내부 도구)
- **목적**: 태일님(대표)·정열님(동료) 두 사람이 같이 쓰는, 법인 고객사 위치·컨택이력·연락처를 지도 위에서 관리하는 영업 도구
- **기술 스택**: 바닐라 JS (프레임워크 없음), 카카오맵 JS API, 카카오 REST API(모빌리티), 다음 우편번호 API, Google Sheets(데이터 저장소), Google Apps Script 웹앱(저장 엔드포인트), SheetJS(xlsx 읽기), ExcelJS(xlsx 양식 생성/쓰기 전용)
- **파일 구조**: 단일 `index.html` 파일 (HTML+CSS+JS 전부 포함, 약 4,800줄+)
- **배포**: GitHub Pages (gtale4102/coway-map)
- **저장 방식**: form POST + 숨김 iframe / JSONP 읽기. 실패 시 `localStorage` 폴백(오프라인 모드)
- **카카오맵 키**: JS API `[REDACTED]`, REST API(모빌리티) `[REDACTED]`

---

## 2. 버전 관리 규칙

- **정수부(메이저)**: 구조적으로 큰 변화 (예: 데이터 모델 전면 개편) → +1
- **소수점 첫째자리**: 중간 규모 기능 추가 → +0.1 (표기는 v5.0이 아니라 v5.00으로 2자리 고정)
- **소수점 둘째자리**: 자잘한 수정·버그픽스 → +0.01
- **현재 버전: v5.01** (설정 화면 헤더에 표시됨)
- 매 작업마다 버전을 올려야 함 — 작업 시작 시 반드시 현재 버전 확인하고 진행

---

## 3. 데이터 구조 (★ 가장 중요)

### 3.1 v5.00 이전 구조 (구버전, 마이그레이션 대상)
```
{
  contacts: [ { name, phone, addressId } ],   // addressId 있으면 특정 주소 전용, 없으면 "공용"
  addresses: [ { id, label, address, detail, lat, lng } ]
}
```

### 3.2 v5.00 이후 현재 구조 (★ 이 구조로 전면 개편됨)
```
{
  id, name, category, memo,
  addresses: [
    {
      id,           // 고유 id: addr_<timestamp>_<rand>
      label,        // 주소구분 (예: "본사", "공장")
      address,
      detail,
      lat, lng,
      contactName,  // ★ 담당자명 — 주소 블록 안으로 통합됨 (v5.00 변경)
      contactPhone  // ★ 담당자 전화번호 — 주소 블록 안으로 통합됨 (v5.00 변경)
    }
  ],
  lat, lng,         // 대표 좌표 (보통 첫 주소)
  owner,            // '태일' | '정열'
  logs: [ { type, date, memo, addressId } ],
  createdAt
}
```

### 3.3 핵심 설계 원칙
- **담당자는 주소마다 1명**: 담당자가 다르면 → 다른 주소 블록. 주소도 다르고 담당자도 완전히 다른 경우는 애초에 다른 고객사로 분리하는 것이 원칙.
- **contacts[] 배열은 v5.00부터 완전히 제거됨**. 구버전 데이터는 앱 로드 시 `migrateContactsToAddresses()`가 자동으로 마이그레이션:
  - addressId 있는 contact → 해당 주소의 contactName/contactPhone
  - addressId 없는 contact → 첫 번째 주소에 귀속
  - 마이그레이션 후 contacts[] = [] 로 비움
- **"전체 공용 담당자"나 "공용 담당" 개념은 존재하지 않음** — v5.00에서 완전히 삭제됨. 과거 인수인계 md(대화02)에 이 개념이 남아 있으나 현재는 무효.
- 주소 id: `ensureAddressIds()`가 앱 로드 시 과거 데이터에 자동 부여

### 3.4 마이그레이션 함수 (참고용)
```javascript
function migrateContactsToAddresses() {
  let changed = false;
  customers.forEach(c => {
    if (!c.contacts || c.contacts.length === 0) return;
    const alreadyMigrated = (c.addresses || []).some(a => a.contactName);
    if (alreadyMigrated) { c.contacts = []; changed = true; return; }
    const addrs = c.addresses || [];
    c.contacts.forEach(ct => {
      if (!ct.name && !ct.phone) return;
      let targetAddr = null;
      if (ct.addressId) targetAddr = addrs.find(a => a.id === ct.addressId);
      if (!targetAddr) targetAddr = addrs[0];
      if (targetAddr) {
        if (!targetAddr.contactName) targetAddr.contactName = ct.name || '';
        if (!targetAddr.contactPhone) targetAddr.contactPhone = ct.phone || '';
      }
      changed = true;
    });
    c.contacts = [];
  });
  return changed;
}
```

---

## 4. UI/UX 구조 변경 (v5.00~v5.01)

### 4.1 고객사 추가/수정 폼 (form-sheet)
- **주소 블록 구조**: 주소구분, 주소(검색), 상세주소, 담당자명, 담당자 전화번호가 하나의 블록으로 묶임
- **"위와 동일한 담당자" 체크박스 (v5.01 신규)**: 두 번째 주소부터 각 주소 블록 하단에 표시. 체크 시 바로 위 주소의 담당자 정보를 복사, 해당 입력필드는 비활성화(disabled+opacity 0.5). 체크 해제 시 필드 초기화 및 재활성화.
```javascript
function applySameContact(checkbox) {
  const wrap = checkbox.closest('.addr-wrap');
  const allWraps = [...document.querySelectorAll('#f-addresses .addr-wrap')];
  const idx = allWraps.indexOf(wrap);
  const nameInput = wrap.querySelector('.addr-contact-name');
  const phoneInput = wrap.querySelector('.addr-contact-phone');
  if (checkbox.checked && idx > 0) {
    const prevWrap = allWraps[idx - 1];
    nameInput.value = prevWrap.querySelector('.addr-contact-name')?.value || '';
    phoneInput.value = prevWrap.querySelector('.addr-contact-phone')?.value || '';
    nameInput.disabled = true; phoneInput.disabled = true;
    nameInput.style.opacity = '0.5'; phoneInput.style.opacity = '0.5';
  } else {
    nameInput.disabled = false; phoneInput.disabled = false;
    nameInput.style.opacity = '1'; phoneInput.style.opacity = '1';
    nameInput.value = ''; phoneInput.value = '';
  }
}
```

### 4.2 고객사 목록 폴더화 (v5.00 신규)
- **같은 고객사명끼리 📁 폴더로 묶음**: 고객사명이 동일한 항목이 여러 개일 때 폴더로 그룹화
- **기본: 접힘 상태**. 폴더 클릭 시 펼침/접힘 토글
- **폴더 상태 관리**: `listFolderState` 객체 (키: 고객사명, 값: true/false)
- **고객사명이 유일하면** 폴더 없이 단일 항목으로 직접 표시
- **분류 색상**: 폴더 자체에는 분류색 표시 없음 (하위 항목이 서로 다른 분류일 수 있어서)
- `renderListBody()` 함수에서 처리

### 4.3 검색창 동작 변경 (v5.01)
- **기존**: 검색 결과에서 선택 → 상세화면(detail-sheet) 열림
- **변경**: 검색 결과에서 선택 → 상세화면 안 열고 지도 핀 위치로 이동만 함
- **주소가 여러 개인 경우**: 결과에서 고객사명 클릭 시 주소 목록 펼쳐짐(아코디언), 개별 주소 선택 시 그 좌표로 이동
```javascript
function selectSearchResultAddr(custId, addrId) {
  document.getElementById('search-results').style.display = 'none';
  document.getElementById('search-input').value = '';
  const c = customers.find(x => x.id === custId);
  if (!c) return;
  let lat = c.lat, lng = c.lng;
  if (addrId) {
    const addr = (c.addresses || []).find(a => a.id === addrId);
    if (addr && addr.lat) { lat = addr.lat; lng = addr.lng; }
  }
  if (lat && lng) {
    map.setCenter(new kakao.maps.LatLng(lat, lng));
    map.setLevel(4);
  }
}
```

---

## 5. 버그 수정 내역 (v4.02)

### 5.1 "지도에서 보기" 선택 주소 반영 안되는 버그 수정
- **문제**: 상세화면에서 주소 선택 후 "지도에서 보기" 클릭 시 선택한 주소가 아닌 대표 첫 주소로 이동
- **원인**: `moveToMarker(id)` 함수가 addressId 인자를 안 받았음
- **수정**: `moveToMarker(id, addressId)` 로 수정, 해당 addressId의 좌표로 이동
```javascript
function moveToMarker(id, addressId) {
  const c = customers.find(x => x.id === id);
  if (!c) return;
  let lat = c.lat, lng = c.lng;
  if (addressId) {
    const addr = (c.addresses || []).find(a => a.id === addressId);
    if (addr && addr.lat) { lat = addr.lat; lng = addr.lng; }
  }
  if (!lat) { showToast('주소 또는 좌표가 없습니다'); return; }
  map.setCenter(new kakao.maps.LatLng(lat, lng));
  map.setLevel(4);
  closeAllSheets();
}
```

### 5.2 네비게이션 전단계 회귀 버그 수정
- **문제**: 목록→상세→수정→저장/취소 시 전단계(상세 또는 목록)로 복귀하지 않고 모두 꺼지는 문제
- **수정**: `closeFormSheet()`, `closeDailySheet()` 함수 추가, `dailySheetOrigin` 변수 추가
```javascript
let dailySheetOrigin = null;

function closeFormSheet() {
  closeSheet('form-sheet');
  if (editingCustomerId) {
    setTimeout(() => openDetail(editingCustomerId, detailSheetOrigin, detailSelectedAddrId), 50);
  } else if (detailSheetOrigin === 'list') {
    setTimeout(() => openSheet('list-sheet'), 50);
  }
}

function closeDailySheet() {
  closeSheet('daily-sheet');
  if (dailySheetOrigin && dailySheetOrigin.startsWith('detail:')) {
    const parts = dailySheetOrigin.split(':');
    const custId = parts[1];
    const addrId = parts[2] || null;
    setTimeout(() => openDetail(custId, detailSheetOrigin, addrId || null), 50);
  } else if (detailSheetOrigin === 'list') {
    setTimeout(() => openSheet('list-sheet'), 50);
  }
  dailySheetOrigin = null;
}
```

---

## 6. 이전 대화(대화01~02)에서 이어받은 주요 기능 (v4.01까지)

> 아래는 현재도 유효한 기능들. 상세 구현은 대화02 인수인계 md 참고.

- **핀 겹침 펼치기(spiderfy)**: 픽셀 기준, 인원수 비례 반지름, 좌우 방향 보존
- **라벨 충돌 회피**: 핀+라벨 장애물 등록, 시야 무리 개념, 연결선(leader line)
- **클러스터링**: MarkerClusterer, minLevel 9, 병합핀 유령마커로 숫자 보정
- **핀 병합**: 4m 이내 다른 고객사 → 중립 회색 병합핀, 클릭 시 선택 팝업
- **데이터 분리(split)**: 주소별 분리 기능, addressId 기반 로그 이동
- **데이터 병합(merge)**: 2개 선택 후 병합, 분류 다르면 사용자가 선택
- **엑셀 가져오기**: SheetJS, 컨택유형 미상 시 건너뜀(임의 기본값 금지)
- **엑셀 양식 다운로드**: ExcelJS, 드롭다운 적용
- **동선짜기**: 카카오 모빌리티 REST API
- **현재위치 이동**: geolocation + goToMyLocation()
- **PC 레이아웃**: 380~440px 고정폭, 가운데 정렬
- **컨택기록**: daily-sheet, 주소 선택 드롭다운(주소 2개 이상 시)

---

## 7. 분류(category) 관련 주의사항

- 코드의 `DEFAULT_CATEGORIES`(초기 시드값)와 **실제 운영 분류는 다름**
- **태일님 실제 운영 분류**: `계약 중 / 친밀 / 보통 / 소원 / 발굴·가망 / 거점 / 미분류` 7개
- "미분류"는 `UNCATEGORIZED_ID`(`cuc`)로 코드에 고정 — 삭제 불가
- 다중 분류 시스템은 **의도적으로 보류** 중

---

## 8. GitHub Pages 배포 관련

- 배포 방법: 수정된 `index.html`을 GitHub 저장소(gtale4102/coway-map)에 push → Actions에서 자동 배포
- **GitHub Pages 서버 성능 저하 시 Timeout 발생 가능** (githubstatus.com에서 "Degraded Performance" 확인 가능)
  - 해결: Actions 탭 → Re-run failed jobs 재시도
- Claude는 GitHub에 직접 접근 불가 (GitHub MCP/커넥터 없음) — 코드 작업 후 파일 다운로드해서 직접 push해야 함

---

## 9. 작업 방식 관련 주의사항

- **Claude는 GitHub에 직접 접근 불가**: 코드 수정 → outputs 폴더에 파일 저장 → 태일님이 직접 다운로드 → GitHub push
- **Cowork 환경**: bash 워크스페이스가 가끔 다운됨 → Read/Edit 파일 도구로 직접 작업으로 전환 가능
- **Grep 도구 한글 패턴 문제**: 한글 포함 패턴 검색 시 매칭 실패할 수 있음 → bash grep 또는 python3으로 우회
- **확인 안 받고 작업 진행 금지**: "어때?"처럼 의견만 묻는 질문에 바로 코드 작업하면 안 됨. 지시와 의견 질문을 명확히 구분해서 응답할 것

---

## 10. 보류/미결 항목

- **엑셀 가져오기 덮어쓰기/병합/추가 로직**: 태일님이 로직을 더 생각해보겠다고 보류 중. 현재는 가져올 때마다 모든 기존 데이터에 추가되는 방식으로 동작.
- **다중 분류(태그형)**: 의도적으로 보류. 필요성이 느껴질 때 별도 작업.
- **종 모양 알림센터**: 아이디어 단계에서 보류.
- **수정 저장 후 목록 복귀 여부**: 현재는 지도로 복귀. 목록으로 가야 하는지 미결.

---

## 11. 다음 작업 시 체크리스트

1. 사용자가 올리는 최신 `index.html`이 **v5.01 이후 버전**인지 확인 (설정 화면 헤더에서 버전 확인)
2. 코드 수정 필요 시 최신 `index.html` 파일을 반드시 받아서 작업
3. **데이터 구조는 v5.00 기준** — contacts[] 배열 없음, addresses[].contactName/contactPhone 사용
4. **"공용 담당자" 개념 없음** — 구버전 인수인계 md(대화02)의 해당 내용은 무효
5. 작업 시작 전 범위 상의 및 승인 후 진행
6. 버전 번호 갱신 잊지 말 것
7. 공간적/시각적 레이아웃 작업 시 직접 렌더링으로 확인 후 코드 반영
