# 고객사 지도 앱 — 작업 인계 요약 (v5.20 기준)

이 문서는 직전 대화창(대화03 → 대화04)에서 이뤄진 모든 논의·결정·구현을 포함한 전체 맥락입니다. 새 대화창에서는 이 문서 하나만 보고 바로 이어서 작업할 수 있어야 합니다.

---

## 1. 프로젝트 개요

- **이름**: 고객사 지도 (코웨이 B2BC 영업용 내부 도구)
- **목적**: 태일님(대표)·정열님(동료) 두 사람이 같이 쓰는, 법인 고객사 위치·컨택이력·연락처를 지도 위에서 관리하는 영업 도구
- **기술 스택**: 바닐라 JS (프레임워크 없음), 카카오맵 JS API, 카카오 REST API(모빌리티), 다음 우편번호 API, Google Sheets(데이터 저장소), Google Apps Script 웹앱(저장 엔드포인트), SheetJS(xlsx 읽기), ExcelJS(xlsx 양식/내보내기 생성·쓰기 전용)
- **파일 구조**: 단일 `index.html` 파일 (HTML+CSS+JS 전부 포함, 약 6,000줄+)
- **배포**: GitHub Pages (gtale4102/coway-map)
- **저장 방식**: form POST + 숨김 iframe / JSONP 읽기. 실패 시 `localStorage` 폴백(오프라인 모드)
- **카카오맵 키**: JS API `[REDACTED]`, REST API(모빌리티) `[REDACTED]`
- **대화04(이번 세션) 작업 환경**: 이번엔 Claude Code(로컬 파일 직접 접근 가능한 CLI)에서 진행됨 — `index.html`을 직접 읽고 수정했고, 로컬 `python -m http.server`로 파일을 띄운 뒤 브라우저 자동화 도구로 실제 라이브 Google Sheets 데이터를 대상으로 직접 검증까지 수행함(아래 4번 참고). 다음 세션이 claude.ai(cowork) 환경이면 "Claude는 GitHub에 직접 접근 불가" 원칙(9번)이 다시 적용되지만, Claude Code 환경이면 이번처럼 직접 파일 수정 + 브라우저 실검증이 가능하고 더 안전함.

---

## 2. 버전 관리 규칙

- **정수부(메이저)**: 구조적으로 큰 변화 (예: 데이터 모델 전면 개편) → +1
- **소수점 첫째자리**: 중간 규모 기능 추가 → +0.1 (표기는 v5.0이 아니라 v5.00으로 2자리 고정)
- **소수점 둘째자리**: 자잘한 수정·버그픽스 → +0.01
- **현재 버전: v5.20** (설정 화면 헤더에 표시됨, 대화04에서 v5.12→v5.20으로 올림 — "엑셀 내보내기+재가져오기" 신규 기능 추가는 중간 규모 기능 추가로 판단해 소수점 첫째자리를 올림)
- 매 작업마다 버전을 올려야 함 — 작업 시작 시 반드시 현재 버전 확인하고 진행

---

## 3. 데이터 구조 (★ 가장 중요, 대화03과 동일 — 변경 없음)

### 3.1 customer 객체 구조 (v5.00 이후, 현재도 유효)
```
{
  id, name, category, memo,
  addresses: [
    {
      id,           // 고유 id: addr_<timestamp>_<rand>
      label,        // 주소구분 (예: "본사", "공장")
      address,
      detail,
      lat, lng,
      contactName,  // 담당자명 — 주소 블록 안으로 통합됨(v5.00)
      contactPhone  // 담당자 전화번호 — 주소 블록 안으로 통합됨(v5.00)
    }
  ],
  lat, lng,         // 대표 좌표 (보통 첫 주소)
  owner,            // '태일' | '정열'
  logs: [ { type, date, memo, addressId } ],
  createdAt
}
```

### 3.2 핵심 설계 원칙 (변경 없음)
- **담당자는 주소마다 1명**. `contacts[]` 배열은 v5.00부터 완전히 제거됨(레거시 레코드는 `migrateContactsToAddresses()`가 앱 로드 시 자동 이전).
- **"전체 공용 담당자" 개념 없음.**
- **대화04에서 실제 라이브 데이터로 재확인(2026-07-04)**: 전체 46개 고객사 중 `contacts[]`에 값이 남아있는 건 **0건** — 전부 마이그레이션 완료 상태. `addresses[]` 총 64개 중 37개는 `contactName`/`contactPhone` 값이 있고, 27개는 그 키 자체가 없음(담당자 미배정 주소 — **정상 상태**, 버그 아님). 이 특성은 문자발송 자동화 쪽 스키마 안전장치 설계 근거로도 쓰였음(4번 참고).

### 3.3 마이그레이션 함수 (변경 없음, 참고용)
```javascript
function migrateContactsToAddresses() {
  let changed = false;
  customers.forEach(c => {
    if (!c.contacts || c.contacts.length === 0) return;
    const alreadyMigrated = (c.addresses || []).some(a => a.contactName);
    if (alreadyMigrated) { c.contacts = []; changed = true; return; }
    const addrs = c.addresses || [];
    c.contacts.forEach(ct => {
      if (!ct.name && !ct.phone) return;
      let targetAddr = null;
      if (ct.addressId) targetAddr = addrs.find(a => a.id === ct.addressId);
      if (!targetAddr) targetAddr = addrs[0];
      if (targetAddr) {
        if (!targetAddr.contactName) targetAddr.contactName = ct.name || '';
        if (!targetAddr.contactPhone) targetAddr.contactPhone = ct.phone || '';
      }
      changed = true;
    });
    c.contacts = [];
  });
  return changed;
}
```

---

## 4. 이번 대화(대화04)에서 변경된 내용

### 4.1 관련 프로젝트 컨텍스트 — 문자발송 자동화 스키마 사고 (지도앱 코드 변경 없음, 배경 지식으로 꼭 알아둘 것)
같은 세션에서 `D:\코웨이1\B2BC대리점\고객사 관리 문자 발송 자동화`(문자발송 시스템, tkinter+Aligo SMS, 별도 프로젝트)의 `연락처저장소.py`가 이 지도앱의 Google Sheets 데이터를 **읽기 전용**으로 연동하고 있다는 걸 확인했는데, 지도앱이 v5.00으로 스키마를 바꾼 뒤(`contacts[]`→`addresses[].contactName/contactPhone`) 문자발송 쪽 코드가 이를 못 따라가 **연락처 조회가 전면 무응답**이었던 사고를 발견·수정함(문자발송 쪽 파일만 수정, 지도앱은 무수정).
- **왜 지도앱 인수인계 문서에 남기는가**: 앞으로 지도앱의 `customers[]`/`addresses[]` 구조를 또 바꾸면 이 사고가 재발할 수 있음. 문자발송 쪽에 `SCHEMA_CONTRACT.md`(의존 필드 목록)와 `MapSchemaError`(고객사 전체에서 담당자 연락처를 단 하나도 못 찾으면 자동으로 명확히 실패하는 안전망)를 만들어뒀으니, **지도앱에서 `owner`/`addresses[].contactName`/`contactPhone`/`name`/`category`/`memo`/`id` 필드의 이름을 바꾸거나 삭제할 계획이 있으면, 문자발송 쪽 `SCHEMA_CONTRACT.md`를 먼저 확인하고 같은 세션에서 `연락처저장소.py`도 함께 갱신할 것.**
- 상세 내용은 `고객사 관리 문자 발송 자동화/SCHEMA_CONTRACT.md`와 그쪽 세션 기록 참고.

### 4.2 엑셀 내보내기 + 재가져오기(수정용) 신규 기능 — 지도앱 코드 변경, 완료+실검증됨
**배경**: 기존 "고객사 일괄 가져오기"는 신규 추가 전용(미리보기 없음, 이름 기준 매칭, 중복 감지 없음)이라 기존 고객사를 엑셀로 대량 수정하는 용도로는 못 씀. 이를 보완하는 완전히 새로운 별도 워크플로우를 추가함.

**설계 원칙(설계안 승인 과정에서 확정)**:
1. 완전 동일 데이터는 자동 스킵(비교해서 다른 값이 있을 때만 미리보기에 표시)
2. 내보내기 시 숨김 컬럼(`_고객사ID`/`_주소ID`/`_체크섬`)으로 이름이 아니라 ID 기준 매칭
3. 삭제는 절대 자동 실행 안 함 — 미리보기에서 사용자가 명시적으로 체크해야만 반영(기본 체크 해제)
4. 컨택이력(`logs[]`)·좌표·생성일은 이 워크플로우가 절대 안 건드림(매칭된 레코드는 그대로 보존)
5. 주소 텍스트가 실제로 바뀐 행만 재지오코딩, 안 바뀌면 기존 좌표 재사용

**핵심 구현(전부 `index.html`에 있음, 함수명 그대로 검색 가능)**:
- `buildExportRows(customers, categories)`: customers[]로부터 (메인 시트 행 목록, 메타 스냅샷 목록) 생성. 순수 함수.
- `_simpleChecksum(value)`: 고객사 필드+주소 필드를 합쳐 만드는 변경감지용 해시(암호학적 용도 아님). 각 행에 내보내기 시점 값으로 박아둠 — **재가져오기 파일의 편집된 값이 아니라, 그 행이 가리키는 라이브 레코드를 "지금 다시" 계산한 체크섬과 비교**해서 "내보내기 이후 다른 곳에서 바뀌었는지" 감지함(이 방향을 반대로 하면 사용자의 정상적인 편집까지 전부 오탐으로 "충돌" 처리되는 버그가 나므로 주의).
- `computeReimportDiff(mainRows, metaRows, liveCustomers, liveCategories)`: 추가/수정/삭제/충돌 후보를 계산하는 핵심 순수 함수(DOM·네트워크 의존 없음). **동시 편집 대응의 핵심**: 삭제 후보는 `liveCustomers`가 아니라 `metaRows`(내보내기 시점 스냅샷, `_메타` 숨김시트에 저장)에 있었는지로 판단 — 내보내기 이후 다른 사람이 추가한 데이터는 애초에 메타에 없으므로 삭제 후보에 절대 안 들어감.
- `exportCustomerDataForEdit()`: ExcelJS로 메인 시트(숨김 컬럼 포함)+안내 시트+`_메타`(`state='veryHidden'`, 엑셀 UI에서 우클릭으로도 숨김 해제 안 됨) 3개 시트 생성 후 다운로드.
- `handleReimportFile(e)` → `renderReimportPreview(diff)`: 파일 업로드 → 4개 그룹(➕추가/✏️수정/🗑️삭제/⚠️충돌) 체크박스 미리보기. 삭제·충돌은 기본 체크 해제.
- `applyReimportChanges()`: "반영" 클릭 시 **라이브 데이터를 한 번 더 fetch해서 `computeReimportDiff`를 재실행**(미리보기를 띄워둔 사이의 변경까지 재검증) → 체크된 항목만 반영. 고객사 전체 삭제는 기존 `deleteCustomer()`와 동일한 소프트 삭제(`markDeleted()`+`trash[]` 이동, 하드 삭제 없음). 주소 단위 삭제/추가/수정은 매칭된 고객사 객체를 직접 mutate.
- 새 UI: 설정 탭에 "고객사 데이터 내보내기(수정용)" / "재가져오기(수정용)" 버튼 2개, 새 바텀시트 `reimport-sheet`.

**검증(전부 완료)**:
- Node.js가 없는 환경이라 브라우저 JS 콘솔로 diff 로직 단위 테스트(완전동일/이름수정/동시추가-삭제후보제외/동시수정-충돌분류/신규+주소추가+주소삭제 혼합, 총 20개 assertion 전부 통과).
- 실제 `index.html`을 로컬 HTTP 서버로 띄워 **실제 라이브 데이터(46개 고객사, 64개 주소)**로 export→SheetJS 재파싱→diff 왕복 검증(0건 변경 확인).
- **실제 반영까지 실검증**: 테스트 전용 고객사 2건(`ZZ테스트_작업D_수정용`/`ZZ테스트_작업D_삭제용`)을 만들어, 수정 케이스(메모변경+주소추가)와 삭제 케이스(소프트삭제) 둘 다 미리보기→체크→반영까지 실행하고, **실제 구글시트를 재조회해서 반영됨을 확인**. 이후 두 테스트 고객사 전부 정리, 최종 고객사 수가 원래 46건으로 복귀했음을 재확인함.

---

## 5. 이전 대화에서 이어받은 주요 기능

> 아래는 현재도 유효한 기능들. 상세 구현은 대화01~03 인수인계 md 참고.

- 핀 겹침 펼치기(spiderfy), 라벨 충돌 회피, 클러스터링, 핀 병합(4m 이내)
- 데이터 분리(split)/병합(merge)
- 엑셀 가져오기(SheetJS, 신규 추가 전용) / 엑셀 양식 다운로드(ExcelJS)
- 동선짜기(카카오 모빌리티 REST API), 현재위치 이동
- PC 레이아웃(380~440px 고정폭), 컨택기록(daily-sheet)
- 고객사 목록 폴더화(같은 이름끼리 묶음), "위와 동일한 담당자" 체크박스
- 검색 결과 선택 시 상세화면 대신 지도 이동만
- 분류: 태일님 실제 운영 7개(계약 중/친밀/보통/소원/발굴·가망/거점/미분류), 다중 분류는 보류 중

---

## 6. 작업 방식 관련 주의사항

- **환경에 따라 다름**: claude.ai(cowork) 환경이면 GitHub 직접 접근 불가 → 코드 수정 후 outputs 폴더에 저장 → 사용자가 직접 다운로드 → GitHub push. **Claude Code(CLI, 로컬 파일 접근 가능) 환경이면 대화04처럼 직접 파일 수정 + 로컬 서버로 브라우저 실검증까지 가능** — 이쪽이 더 안전하고 확실하므로 가능하면 이 방식을 우선할 것.
- Grep 도구 한글 패턴 문제: 한글 포함 패턴 검색 실패 시 다른 검색 방식으로 우회.
- **확인 안 받고 작업 진행 금지**: 의견만 묻는 질문에 바로 코드 작업하지 말 것. 특히 삭제를 수반하는 로직·스키마 변경은 설계안을 먼저 보고하고 승인받은 뒤 구현할 것(대화04에서 실제로 이 절차를 따름 — 작업 D 설계안 제시 → 승인 → 구현 → 재검증 순서).
- 실 라이브 데이터로 반영 테스트를 할 때는 **반드시 테스트 전용 더미 레코드**를 만들어서 검증하고, 테스트 후 완전히 정리(원래 데이터 건수 재확인)할 것 — 실제 고객사 데이터를 테스트 대상으로 쓰지 말 것.
- 공간적/시각적 레이아웃 작업 시 직접 렌더링으로 확인 후 코드 반영.

---

## 7. 보류/미결 항목

- **엑셀 가져오기(기존, 신규추가용) 덮어쓰기/병합/추가 로직**: 태일님이 로직을 더 생각해보겠다고 보류 중.
- **다중 분류(태그형)**: 의도적으로 보류.
- **종 모양 알림센터**: 아이디어 단계에서 보류.
- **NEW — 기존 "엑셀 일괄 가져오기"(신규 추가 전용)가 여전히 구식 `contacts[]`에 담당자를 씀**(`processExcelImport` 함수): 마이그레이션 함수가 다음 로드 때 자동으로 옮겨주긴 하지만, 처음부터 `addresses[].contactName/contactPhone`에 직접 쓰도록 정리하는 게 더 깔끔함. 대화04에서 발견했지만 이번 범위(내보내기/재가져오기 신규 기능) 밖이라 손 안 댐 — 사용자가 "지금 당장 문제 없음"이라 확인, 다음에 별도로 처리.
- **NEW — 휴지통 완전삭제 시 병합 로직이 되살릴 수 있는 특성(기존 버그, 대화04에서 발견)**: `mergeAndSaveToSheet()`가 `customers`/`routeHistory`/`manualFolders`는 삭제 tombstone(`DELETED_IDS_KEY` 등)으로 보호하지만, **`trash`(휴지통) 자체에는 tombstone이 없음**. 그래서 `permanentlyDeleteFromTrash()`로 휴지통에서 완전삭제해도, 그 직후 다른 기기/세션에서 저장이 일어나 원격에 아직 남아있던 옛 trash 데이터를 병합해오면 방금 지운 항목이 되살아날 수 있음. 대화04에서 테스트 데이터 정리 중 실제로 겪음(우회 방법: `saveTrash()`의 병합 단계를 거치지 말고 `saveToSheetViaForm()`으로 직접 payload를 덮어써서 정리 — 다만 이건 "동시 저장 충돌 방지"라는 병합의 원래 목적을 우회하는 임시방편이라 정식 수정은 아님). **다음에 고칠 것**: `customers` 등과 동일하게 `trash` 항목 완전삭제 시에도 별도 tombstone 키(예: `PURGED_TRASH_IDS_KEY`)를 두고 병합 시 걸러내도록 `mergeAndSaveToSheet()`/`permanentlyDeleteFromTrash()`를 수정. 사용자 지시로 이번 세션엔 손대지 않음.

---

## 8. 다음 작업 시 체크리스트

1. 사용자가 올리는(또는 로컬의) `index.html`이 **v5.20 이후 버전**인지 확인 (설정 화면 헤더에서 버전 확인)
2. 코드 수정 필요 시 최신 `index.html` 파일을 반드시 받아서 작업
3. 데이터 구조는 v5.00 기준(3번 섹션) 그대로 — 변경 계획이 있으면 문자발송 자동화의 `SCHEMA_CONTRACT.md`부터 확인(4.1번 참고)
4. 작업 시작 전 범위 상의 및 승인 후 진행, 특히 삭제/스키마 변경은 설계안 먼저
5. 버전 번호 갱신 잊지 말 것
6. 공간적/시각적 레이아웃 작업 시 직접 렌더링으로 확인 후 코드 반영
7. Claude Code 환경이면 로컬 서버(`python -m http.server`)로 띄워 브라우저 자동화로 직접 검증하는 방식을 우선 고려(대화04 방식) — 실 데이터 반영 테스트 시 반드시 더미 레코드 사용 후 정리
8. 7번 항목(엑셀 가져오기 구식 스키마) / 휴지통 tombstone 부재 건은 다음에 여유 있을 때 처리 대상
