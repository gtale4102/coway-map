# 고객사 지도 앱 — 작업 인계 요약 (v4.01 기준)

이 문서는 직전 대화창에서 이뤄진 모든 논의·결정·구현을 빠짐없이 정리한 것입니다. 새 대화창에서는 이 문서 하나만 보고 바로 이어서 작업할 수 있어야 합니다.

---

## 1. 프로젝트 개요

- **이름**: 고객사 지도 (코웨이 B2BC 영업용 내부 도구)
- **목적**: 태일님(대표)·정열님(동료) 두 사람이 같이 쓰는, 법인 고객사 위치·컨택이력·연락처를 지도 위에서 관리하는 영업 도구
- **기술 스택**: 바닐라 JS (프레임워크 없음), 카카오맵 JS API, 카카오 REST API(모빌리티), 다음 우편번호 API, Google Sheets(데이터 저장소), Google Apps Script 웹앱(저장 엔드포인트), SheetJS(xlsx 읽기), ExcelJS(xlsx 양식 생성/쓰기 전용)
- **파일 구조**: 단일 `index.html` 파일 (HTML+CSS+JS 전부 포함, 4,780줄 이상)
- **배포**: GitHub Pages (gtale4102/coway-map)
- **저장 방식**: form POST + 숨김 iframe / JSONP 읽기. 실패 시 `localStorage` 폴백(오프라인 모드)
- **카카오맵 키**: JS API `[REDACTED]`, REST API(모빌리티) `[REDACTED]`

## 2. 버전 관리 규칙 (이번 대화에서 새로 도입)

- 과거 날짜식 버전(`v260619-XX`) → 이번 대화 중 `v2.80`으로 리셋, 이후 시맨틱 버전 체계 적용
- **정수부(메이저)**: 구조적으로 큰 변화 (예: 데이터 모델 전면 개편) → +1
- **소수점 첫째자리**: 중간 규모 기능 추가 → +0.10
- **소수점 둘째자리**: 자잘한 수정 → +0.01
- **현재 버전: v4.01** (설정 화면 헤더에 표시됨, `<div class="sheet-title">설정 <span>...v4.01</span></div>`)
- 매 작업마다 빠짐없이 버전을 올려야 함 — 작업 시작 시 반드시 확인하고 진행

## 3. 데이터 구조 (★ 가장 중요 — v4.00에서 전면 개편됨)

### 3.1 고객사(customer) 객체
```
{
  id, name, category, memo,
  contacts: [ { name, phone, addressId } ],   // addressId 없음(null) = 전체 공용 담당자
  addresses: [ { id, label, address, detail, lat, lng } ],  // 모든 주소는 고유 id 보유
  lat, lng,           // 대표 좌표 (보통 첫 주소)
  owner,              // '태일' | '정열'
  logs: [ { type, date, memo, addressId } ],  // addressId 없음(null) = 전체 공용 이력
  createdAt
}
```

### 3.2 핵심 설계 원칙
- **주소(address)에 안정적인 고유 id 부여** (`addr_<timestamp>_<rand>`). 앱 로드 시 `ensureAddressIds()`가 과거 데이터(주소 id 없음)에 자동으로 id를 부여 — 별도 수동 마이그레이션 불필요.
- **로그/연락처의 `addressId`가 비어있으면 "전체 공용"으로 자동 처리.** 이게 곧 과거 데이터 호환 방법 — 과거에 저장된 로그는 모두 addressId가 없으므로 자동으로 "공용"으로 표시됨. 별도 마이그레이션 로직 없음(설계상 의도된 동작).
- **주소가 삭제되어 연결이 끊긴(orphan) 로그/연락처도 데이터 유실 없이 "공용"으로 자동 취급**됨 (`belongsToAddrFilter` 함수가 "이 addressId를 가진 주소가 지금 존재하는지" 체크 후, 없으면 공용 취급).
- **고객사 수정(폼) 저장 시 주소 id 보존 버그를 발견해서 같이 고침**: 원래는 수정 화면에서 저장할 때마다 주소 배열을 폼에서 완전히 새로 만들어서 id가 매번 사라지는 문제가 있었음. `addAddressRow()`가 폼에 추가되는 즉시(`dataset.addrId`) id를 부여(기존 주소면 보존, 신규면 새로 발급)하도록 수정해서 해결.

### 3.3 분류(category) — ★ 중요한 혼동 주의
- 코드의 `DEFAULT_CATEGORIES` 상수(최초 1회 설치 시 시드값)는 `계약 중/잠재 고객/재계약 예정/휴면/대기업`이지만, **이건 실제 운영 데이터가 아님**.
- **태일님이 실제로 쓰고 있는 살아있는 분류는 (Google Sheets에 저장됨)**: `계약 중 / 친밀 / 보통 / 소원 / 발굴·가망 / 거점 / 미분류` 7개. "미분류"는 `UNCATEGORIZED_ID`(`cuc`)로 코드에 고정되어 삭제 불가.
- 분류 색상도 사용자가 직접 바꿀 수 있음(컬러피커 + 헥스 직접입력, 미리보기 후 "확인" 버튼 눌러야 적용 — 즉시적용 아님).
- **"계약 중" 기본 색상을 `#4C88F5`(카카오맵 기본 핀 색상)로 바꿨지만, 이건 신규 설치 시 초기값만 바뀐 것** — 기존에 저장된 실제 "계약 중" 분류는 자동으로 안 바뀜. 사용자가 분류 관리 화면에서 직접 색을 바꿔야 함.
- **다중 분류(태그형, 동시에 여러 분류 가능)는 명시적으로 보류됨.** 병합 시 분류가 다르면 자동 규칙 없이 사용자가 그 자리에서 직접 선택하게 구현(아래 6.4 참고). 다중분류 시스템 자체는 "필요성이 느껴질 때 별도 작업"으로 미룸 (Claude 메모리에 저장된 규칙).

## 4. 동선짜기 / 위치 관련

- 동선짜기 버튼(🗺️, `.route-fab`, 우측 하단 고정)은 기존 기능, 이번엔 텍스트 스타일만 수정: 동선 요약("총 XXkm · 약 XX분")을 `font-size:14px, font-weight:700, color:var(--accent)`로 변경(기존엔 흐린 회색 12px라 안 보였음).
- **현재 위치 이동 버튼 신규 추가** (`.locate-fab`, 동그라미+점 SVG 아이콘, route-fab 바로 위). `goToMyLocation()` → `navigator.geolocation.getCurrentPosition` → 지도 중심 이동 + 기존 `updateMyLocation()` 재사용해 파란 점 갱신. PC에서도 동일하게 노출(완전 정확하진 않아도 대략 확인 가능하다는 이유).

## 5. 지도 핀 / 라벨 — 가장 많은 시행착오를 거친 영역

### 5.1 핵심 교훈 (★ Claude 메모리에 영구 저장됨, 규칙 #22)
> 핀 배치·라벨 겹침 회피 등 여러 요소가 2D 화면에서 서로 어떻게 보이는지(겹침/교차/배치)가 중요한 공간적·시각적 작업은, 텍스트 추론만으로 코드를 내놓지 말 것. 먼저 실제 알고리즘을 Python 등으로 이식해 샘플 데이터로 직접 렌더링(이미지)해서 Claude가 눈으로 확인하고 문제를 스스로 찾아 고친 뒤에 실제 코드에 반영할 것.

이 규칙이 생긴 이유: 핀 라벨 배치 작업에서 텍스트 추론만으로 여러 번(v2.84~v2.92) 고쳤지만 스크린샷으로 볼 때마다 계속 어긋났음. 결국 Python(`/home/claude/labtest/sim_core.py`, `render.py` 등)으로 실제 알고리즘을 그대로 이식해 PNG 렌더링 → 직접 보고 문제 발견 → 수정하는 방식으로 전환한 뒤에야 제대로 해결됨.

### 5.2 핀 펼치기 (spiderfy) — `spiderfyPixels()`
- **실거리(m) 기준이 아니라 화면 픽셀 기준**으로 겹침 판정 (`PIN_OVERLAP_PX = 30`). 실거리 기준이면 줌 축소 시 간격이 몇 픽셀로 쪼그라들어 다시 겹치는 문제가 있어서 픽셀 기준으로 전환.
- 펼치는 반지름은 무리 인원수에 비례해서 동적 계산 (`PIN_MIN_GAP_PX = 32`, `PIN_MIN_RADIUS_PX = 16`) — 인원이 많아져도(10개 이상) 다시 빽빽해지지 않도록.
- **좌우 뒤바뀜 버그 수정(v2.93)**: 원래 핀을 펼칠 때 "배열에 들어간 순서"대로 좌/우를 배정해서, 실제로는 A가 서쪽인데 줌에 따라 우연히 반대로 표시되는 버그가 있었음 (PC 화면에서 "집(아파트)"와 "코웨이 B2BC" 핀이 줌마다 좌우가 바뀌는 걸로 발견됨, 확대하면 정상으로 보임). 펼치기 전 원래 위치가 무리 중심 기준 몇 시 방향이었는지(`true_angle`)로 정렬 후, 정렬된 첫 항목의 "진짜 각도"를 시작점으로 균등 배치하도록 수정해서 해결.

### 5.3 라벨 충돌 회피 — `findFreeLabelSlot()`
- 라벨이 다른 라벨뿐 아니라 **다른 핀도 가리지 않도록** pre-pass에서 모든 핀 위치를 먼저 장애물로 등록.
- **"시야 무리"(view group) 개념 도입**: 실제 좌표가 멀어도(다른 spiderfy 그룹) 화면 픽셀상 가까우면(`VIEW_NEIGHBOR_PX = 260`) 하나의 무리로 재계산해서 일관되게 부채살로 펼침. 줌 축소 시 "같은 고객사 다른 지점인데 화면상 우연히 붙어있는" 경우의 라벨 꼬임을 해결하기 위함.
- 라벨 탐색은 **거리(반지름) 우선**, 같은 반지름 안에서는 좌우(수평)를 선호. 좌우로 보낼 때 박스를 중앙정렬이 아니라 **방향에 맞는 모서리 기준**으로 배치(안 그러면 폭이 넓은 라벨이 다시 핀 쪽으로 파고드는 문제 있었음).
- 무리별 탐색 범위는 자기 무리의 실제 픽셀 크기로 제한(`maxDist`) — 옆/아래 다른 무리 영역을 침범하지 않도록.
- 연결선(leader line)은 핀의 뾰족한 좌표점이 아니라 **핀 머리(원형) 테두리에서 라벨 방향으로 가장 가까운 지점**에서 시작.
- 라벨이 옮겨진(moved) 핀만 식별 색상 적용: 핀 테두리 + 라벨 테두리 + 연결선을 **같은 색**으로 통일(`LABEL_LINK_PALETTE`, 색상환 기준 10색 균등 배치 — 기존엔 빨강 2개·초록 2개가 섞여 헷갈렸음).
- 안 옮겨진(겹치지 않는) 핀/라벨은 기존처럼 깔끔하게 색 없이 그대로 둠.

### 5.4 핀 크기 / 위치 보정 표시
- 핀 크기 65%로 축소(`PIN_SCALE = 0.65`) — 라벨 배치 공간 확보 목적. 연결선 시작점, 장애물 영역, 리마인드 배지 위치도 전부 같은 비율로 비례 조정.
- **위치가 spiderfy로 보정된(밀려난) 핀은 65% 투명도로 표시** — "이 핀은 실제 정확한 좌표가 아닐 수 있다"는 걸 시각적으로 알리기 위함. 단독 핀(안 밀려난)은 완전 불투명 유지.
  - 배경: 핀이 겹쳐서 펼쳐질 때, 많이 축소된 화면일수록 같은 픽셀 간격이 더 큰 실거리 오차를 의미하게 됨(트레이드오프, 불가피).

### 5.5 같은 주소 다른 고객사 → 핀 병합
- 실거리 `ADDR_MERGE_M = 4`m 이내에 **다른 고객사**(같은 고객사의 다른 주소는 제외)가 있으면 핀 하나로 합쳐서 표시.
- 병합된 핀은 분류색 대신 **중립 회색**(`MERGED_PIN_COLOR = '#8b91b5'`) — 합쳐진 업체들 분류가 제각각이라 색 섞으면 오히려 헷갈림.
- 라벨은 여러 줄로 업체명 나열.
- 클릭하면 `openMergedChooser()` 팝업 떠서 어느 업체를 볼지 선택 (하단시트, `#merged-pin-overlay`/`#merged-pin-list` 재사용 — 같은 DOM을 "병합 핀 선택"과 "고객사 병합 확인" 두 가지 용도로 재사용 중이니 유지보수 시 주의).
- 라벨 텍스트도 클릭하면 동일하게 상세정보(또는 선택 팝업) 열림 — 기존엔 핀만 클릭 가능했음. 라벨을 순수 HTML 문자열이 아니라 DOM 엘리먼트로 만들어서 `addEventListener('click', ...)` 직접 붙이는 방식으로 변경(따옴표 포함 고객사명도 안전하게 처리하기 위함).

### 5.6 클러스터링 (MarkerClusterer)
- 카카오 공식 `kakao.maps.MarkerClusterer` 사용 (`libraries=services,clusterer`로 SDK 로드).
- `minLevel: CLUSTER_MIN_LEVEL = 9` (이 이상 축소되면 클러스터링 시작).
- `gridSize: 38` (기본값보다 작게 — 더 세밀한 단위로 묶이도록, 사용자 요청으로 축소).
- 클러스터 버블 색상: 3단계 직접 지정(`calculator: [10, 30]`) — 2~9개=파랑(`rgba(79,124,255,...)`), 10~29개=보라, 30개+=빨강, 흰 글씨 굵게. (기존엔 카카오 기본 스타일이라 흰 글씨가 잘 안 보이는 문제가 있었음)
- **버블 숫자가 "업체 개수"와 다를 수 있는 문제**: 카카오 클러스터러는 기본적으로 "마커(핀) 개수"를 세는데, 병합 핀(여러 업체가 핀 1개)이 있으면 핀 개수 ≠ 업체 개수가 됨. **해결**: 카카오 `texts` 콜백이 숫자(count)만 받고 마커 목록을 못 받는 API 제약이 있고, `clustered` 이벤트로 직접 텍스트를 덮어쓰는 방법은 클릭이 안 되는 알려진 버그가 있어서(카카오 개발자포럼 미해결) 위험해 사용 안 함. 대신 **병합 핀마다 화면에 안 보이는 1x1 투명 마커("유령 마커")를 (업체수-1)개 같은 위치에 추가**해서 클러스터러가 세는 마커 개수 자체를 실제 업체 수와 일치시킴. 표준 클릭/확대 동작은 전혀 안 건드림. (`GHOST_MARKER_IMAGE`, initMap()에서 1회 생성)
- 클러스터링은 그리드(격자) 기반 표준 방식 — 구글맵 등도 동일. 줌마다 격자가 새로 그어져서 부모-자식 관계가 보존되지 않는 게 모든 그리드 클러스터러의 공통 한계라고 사용자에게 설명함(버그 아님).

## 6. 데이터 분리/병합 기능 (v4.00 핵심 신규 기능)

### 6.1 배경
엑셀로 같은 고객사명+다른 주소 행을 일괄 가져오면 자동으로 "한 고객사, 여러 주소"로 묶이는데, 로그/연락처가 고객사 단위로 공유되다 보니 핀(주소)별로 데이터가 안 갈라지는 문제 발견. 근본 해결을 위해 데이터 구조 자체(3장 참고)를 변경.

### 6.2 분리(split) — `splitAddress(custId, addrId)`
- 상세화면에서 주소 옆 "✂️ 분리" 버튼 (주소가 2개 이상일 때만 표시).
- 그 주소 전용(addressId 일치) 로그/연락처만 그대로 들고 새 고객사로 떨어져 나감. 공용(addressId 없음) 로그/연락처는 원본에 남음.
- 새 고객사는 분류를 원본 그대로 복사, 메모는 비움.
- 확인창 후 즉시 실행 + 저장.

### 6.3 상세화면 주소별 필터링
- 주소가 2개 이상인 고객사는 "전체보기" + 각 주소 탭이 표시됨.
- 특정 주소를 클릭하면 `selectDetailAddress()` → 그 주소(+공용) 데이터만 필터링해서 보여줌 (`belongsToAddrFilter()` 함수).
- 핀을 클릭해서 들어가면 그 핀의 주소가 자동으로 선택된 채로 열림(`openDetail(id, origin, addressId)`).
- 컨택 이력/담당자 섹션 제목에 "(이 주소 + 공용)"이라고 표시해서 필터링 중임을 알림.

### 6.4 병합(merge) — `openMergeConfirm()` / `confirmMergeCustomers()`
- 목록 화면 기존 선택모드(원래 삭제용)에 "🔗 병합" 버튼 추가 — **정확히 2개 선택했을 때만** 노출.
- 분류가 다르면 합치기 전에 라디오 버튼으로 **어느 쪽 분류로 할지 사용자가 직접 선택** (자동 규칙·동시표기 없음 — Claude 메모리 규칙 #25로 저장됨).
- 주소·담당자·이력 전부 합쳐짐, 메모는 다르면 " / "로 이어붙임. 합쳐진 쪽(B)은 삭제됨. 되돌릴 수 없음(확인창에 명시).

### 6.5 컨택기록 입력 시 주소 선택
- 고객사를 선택하면 `populateDailyAddressSelect()`가 주소 개수 확인 → 2개 이상이면 "어느 주소 건인가요?" 드롭다운 표시 (1개뿐이면 안 물어보고 공용으로 기록).
- 핀 클릭 → 상세화면 → "✏️ 컨택 기록" 버튼으로 들어가면, 상세화면에서 선택 중이던 주소가 자동으로 미리 선택됨.
- **검색창 UX 개선(v4.01)**: 고객사 검색 결과에서 주소가 1개뿐이면 기존처럼 이름만 누르면 바로 선택. **주소가 2개 이상이면 "업체명 · N개 주소"로 축약 표시**, 클릭하면 하위에 주소구분 목록이 펼쳐짐(아코디언, ▾/▴ 화살표), 그 중 하나를 누르면 그 주소까지 한 번에 선택+드롭다운 자동 세팅. (처음엔 모든 주소를 풀어서 나열하는 방식이었다가, "업체명+개수 먼저 보여주고 누르면 펼치는" 방식으로 사용자 요청에 따라 변경)

### 6.6 엑셀 가져오기 연동
- 엑셀의 한 행에 주소와 컨택이력(또는 담당자)이 같이 있으면, 그 이력/담당자는 자동으로 그 주소 전용(addressId 연결)으로 처리.
- 주소 없이 이력/담당자만 있는 행은 고객사 전체 공용으로 처리.
- (기존 "고객사명만 보고 그룹화" 로직 자체는 그대로 유지 — 같은 이름이면 여전히 한 고객사로 묶임. 분리/병합 기능으로 가져온 후에 자유롭게 풀고 합칠 수 있게 하는 방향으로 결정됨.)

## 7. 엑셀 가져오기 / 양식 다운로드

### 7.1 가져오기 — `processExcelImport()` (SheetJS 사용, 읽기 전용)
- 인식 컬럼: 고객사명/회사명/상호명, 소유자/담당/영업담당, 분류, 메모/비고, 담당자명/담당자, 연락처/전화번호, 주소구분/구분, 주소, 상세주소/상세, 컨택날짜, 컨택유형, 컨택메모.
- `XLSX.read(..., { cellDates: true })` — 엑셀 날짜 셀이 자동 변환되어 일련번호로 깨지는 문제 방지(직접 발견해서 미리 고침).
- 컨택유형이 방문/전화/메시지 중 명확하지 않으면 **임의로 기본값을 채우지 않고 그 행을 건너뜀** — 건너뛴 항목은 결과 화면에 "⚠️ 컨택유형 미상으로 건너뜀" 목록으로 표시(태일님이 "방문으로 임의 기록되면 혼동 온다"고 지적해서 변경).
- 결과 화면(가져오기 완료 후)은 자동으로 안 사라지는 화면 — "확인" 눌러야 닫힘. 주소 변환 실패 목록, 컨택유형 미상 목록 모두 여기 표시.

### 7.2 양식 다운로드 — `downloadExcelTemplate()` (ExcelJS 사용, 쓰기 전용)
- 설정 화면에 "양식 받기" 버튼.
- "안내" 시트 + "고객사데이터" 시트 2개 구성. 분류·컨택유형 칸에 **드롭다운(데이터 유효성 검사)** 적용 — 분류 목록은 그 순간 실제 등록된 분류를 동적으로 읽어와 반영.
- SheetJS는 드롭다운 생성을 공식 지원 안 해서 ExcelJS를 별도로 추가(읽기는 SheetJS 유지, 쓰기만 ExcelJS — 하이브리드 결정. 전체 교체는 안 함: SheetJS가 구버전 .xls 읽기와 비정형 파일 처리에 더 안정적이라는 이유).
- 독립 파일(`고객사_일괄등록_양식.xlsx`)도 한 번 Python(openpyxl)으로 직접 생성해서 전달한 바 있음 — 실제 운영 중인 분류명(계약 중/친밀/보통/소원/발굴·가망/거점/미분류) 기준으로 만들어짐.

## 8. PC 레이아웃

- 하단시트(`.bottom-sheet`), 주소검색 화면(`#address-search-overlay`), 동선짜기 패널(`.route-panel`)을 PC(700px 이상 화면)에서 고정폭(380~440px)으로 제한 + 가운데 정렬하는 미디어쿼리 추가. 폰 화면은 그대로 전체폭 유지.

## 9. 기타 UI 수정

- "문자" → "메시지" 라벨 전체 변경(7곳, 내부 데이터값 `sms`는 유지).
- 컨택기록 날짜 입력 옆/위에 요일 표시(`updateDateWeekdayLabel()`, `WEEKDAY_KR` 배열). PC 네이티브 날짜 입력 컨트롤이 OS 설정에 따라 빈 괄호 "()"를 표시하는 문제가 있어서(우리 코드 책임 아님, 표준 비제어 영역), input을 투명 처리하고 그 위에 직접 계산한 "YYYY-MM-DD (요일)" 텍스트를 오버레이로 겹쳐서 해결.
- 목록에서 상세정보 보고 X 누르면 다시 목록으로 돌아오게 수정 (`closeDetailSheet()`, `detailSheetOrigin` 변수 — 원래 코드에 이미 있었지만 실제로 안 쓰이고 있었음).

## 10. Claude 메모리에 영구 저장된 이번 대화 관련 규칙 (요약)

1. **공간적·시각적 작업은 반드시 먼저 직접 렌더링해서 눈으로 확인 후 코드 반영** (규칙 #22, 5.1 참고).
2. **고객사 병합 시 분류가 다르면 자동 규칙 없이 사용자가 그 자리에서 직접 선택** (규칙 #25, 6.4 참고). 다중분류 시스템은 필요성이 느껴질 때 별도 작업.

(이 외에도 모델/Effort 추천, 작업 전 상의·승인, Claude Code/스킬/MCP 우선 검토 등 기존 규칙들은 그대로 유효함 — 별도 프로젝트 메모리에 저장되어 있어 새 대화에서도 자동 적용됨.)

## 11. 보류/미해결 항목

- **다중 분류(태그형 분류 시스템)**: 의도적으로 보류. "친밀/보통/소원"처럼 배타적이어야 하는 그룹과 "계약 중/발굴·가망/거점"처럼 동시 가능해야 하는 그룹을 나누는 작업은 핀 색상·필터·범례·분류관리 화면 전체에 영향을 미치는 큰 작업이라 별도로 진행하기로 함.
- **종 모양 알림센터**: 작업 이력·오류·미분류 항목 등을 모아 보는 기능 아이디어가 나왔지만, 설계 요소가 많아(데이터 공유 여부, 보관 기간, 읽음 처리 등) 별도 시간 잡아 진행하기로 하고 보류. 대신 즉시 필요한 부분(컨택유형 미상 등)은 기존 가져오기 결과화면에 통합해서 빠르게 해결함.
- **PC 날짜 입력 빈 괄호의 정확한 원인**: 윈도우 시스템 날짜 형식 설정으로 추정되나 100% 확정은 아님(태일님 PC를 직접 본 게 아니므로). 실용적으로는 오버레이 방식으로 해결돼서 더 이상 영향 없음.
- **수정(edit) 후 저장 시 항상 지도로 복귀하는 동작**: 목록에서 상세보기 → 수정 → 저장하면 (목록이 아니라) 지도로 돌아가는 기존 동작은 그대로 유지됨 — "이어서 목록 보기" 흐름과 약간 안 맞을 수 있다고 짚었으나 바꿀지 여부는 결정 안 됨. 필요시 논의 재개 가능.
- **마지막 작업("어때?"라고 물었는데 바로 진행해버린 건)**: 태일님이 "어때?"라고 의견만 물었는데 Claude가 바로 코드 작업을 진행해버린 일이 있었음(검색결과 펼침 UI). 결과물 자체는 마음에 들어서 그대로 두기로 했지만, **확인 안 받고 진행한 패턴은 다시 반복하지 않도록 주의** — 의견을 묻는 질문과 작업 지시를 명확히 구분해서 응답할 것.

## 12. 다음 작업 시 체크리스트 (새 대화창에서 작업 시작 시)

1. 사용자가 올리는 최신 `index.html`이 v4.01 이후 버전인지 확인 (버전 번호는 설정 화면 헤더에서 확인 가능)
2. 코드 수정이 필요한 작업이면 최신 `index.html` 파일을 반드시 새로 받아서 작업 (이 요약만으로는 정확한 코드 위치를 알 수 없음)
3. 공간적/시각적 레이아웃 관련 요청이면 텍스트 추론만으로 답하지 말고 직접 렌더링해서 확인 후 진행
4. 데이터 구조(addressId 기반 분리) 관련 작업 시, 3장의 구조를 정확히 숙지하고 작업할 것
5. 작업 시작 전 항상 범위를 상의하고 승인받은 후 진행 (특히 데이터 구조나 여러 화면에 영향을 미치는 작업)
6. 버전 번호 갱신 잊지 말 것
